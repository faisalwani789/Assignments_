// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "mysql"
}

model User {
  id         Int      @id @default(autoincrement())
  first_name String
  last_name  String
  email      String   @unique
  password   String
  role       Role     @default(user)
  createdAT  DateTime @default(now())
  isActive   Boolean  @default(true)
  teacher    Teacher?
  student    Student?
}

enum Role {
  user
  student
  teacher
}

model Teacher {
  id                    Int                     @id @default(autoincrement())
  user_id               Int                     @unique
  joining_date          DateTime                @default(now())
  user                  User                    @relation(fields: [user_id], references: [id],map: "fk_user")
  teacherClasses        TeacherClasses[]
}

model TeacherClasses {
  id         Int     @id @default(autoincrement())
  class_id   Int
  teacher_id Int
  isActive  Boolean @default(true)
  class      Class   @relation(fields: [class_id], references: [id])
  teacher    Teacher @relation(fields: [teacher_id], references: [id])
  teacherclassSubjects TeacherClassesSubject[]
}

model TeacherClassesSubject {
  id         Int     @id @default(autoincrement())
  teacherClassId Int
  subject_id Int
  teacher    TeacherClasses @relation(fields: [teacherClassId], references: [id],map: "fk_tcs_teacher_class")
  subject    Subject @relation(fields: [subject_id], references: [id],map:"fk_tcs_subject")

}

model Student {
  id             Int      @id @default(autoincrement())
  user_id        Int      @unique
  Admission_date DateTime @default(now())
  user           User     @relation(fields: [user_id], references: [id])
  roll_no        Int
  class_id       Int      @unique
  class          Class    @relation(fields: [class_id], references: [id])
}

model Class {
  id                    Int                     @id @default(autoincrement())
  className             String
  isActive              Boolean                 @default(true)
  student               Student?
  teacherClasses        TeacherClasses[]
}

model Subject {
  id                    Int                     @id @default(autoincrement())
  subjectName           String
  isActive              Boolean                 @default(true)
  teacherClassesSubject TeacherClassesSubject[]
  teacherSubject TeacherSubject[]
}
model TeacherSubject{
   id         Int  @id @default(autoincrement())
  teacher_id Int
  subject_id Int
  isActive  Boolean @default(true)
  subject      Subject   @relation(fields: [subject_id], references: [id])
  
}
